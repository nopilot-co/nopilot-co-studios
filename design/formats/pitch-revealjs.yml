# Format: pitch-revealjs = purposes/pitch.yml <- exports/revealjs.yml <- overrides
extends: pitch
export: revealjs
overrides:
  # Legacy export, outside the html/pdf/pptx/gslide asset vocabulary — no assets.
  assets: []
