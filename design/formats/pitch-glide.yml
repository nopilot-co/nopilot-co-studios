# Format: pitch-glide = purposes/pitch.yml <- exports/glide.yml <- overrides
# Demonstrates a canonical format whose export is not yet renderable.
extends: pitch
export: glide
overrides:
  # Legacy export, outside the html/pdf/pptx/gslide asset vocabulary — no assets.
  assets: []
